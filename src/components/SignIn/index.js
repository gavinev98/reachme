import React from 'react';

const index = () => {
    return (
        <div className="signInBtn">
            <button>Sign in with google.</button>
        </div>
    );
};

export default index;